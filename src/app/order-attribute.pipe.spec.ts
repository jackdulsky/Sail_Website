import { OrderAttributePipe } from './order-attribute.pipe';

describe('OrderAttributePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderAttributePipe();
    expect(pipe).toBeTruthy();
  });
});
